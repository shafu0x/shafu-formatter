// SPDX-License-Identifier: MIT
pragma solidity ^0.8.26;

contract Escrow {
    function fund()  {
        uint distributionId = distributionIds[i];
        Distribution storage distribution = distributions     [distributionId];
        RepoAccount  memory  repoAccount  = distributionToRepo[distributionId];

        uint         distributionId = distributionIds;
        Distribution distribution   = distributions;
        uint32       distributionId = distributionIds;

        uint                 distributionId = distributionIds[i];
        Distribution storage distribution   = distributions  [distributionId];

        uint         distributionId = distributionIds[i];
        Distribution distribution   = distributions  [distributionId];

        uint         distributionId = distributionIds;
        Distribution distribution   = distributions;
        uint16       distributionId = distributionIds;
    }
}
